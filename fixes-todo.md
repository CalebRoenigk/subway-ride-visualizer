With the current Rides by Line graph, the far right date should be the current days date, right now it only shows the last date there was a logged ride.

can you make the icon for darkmode/light mode animate between the states? The rays of the sun would draw off/on and the center circle would change size and have a bite animate out of it when it turns into the moon and vice versa.

Lets remove this rounding fat border on the page selection titles, lets have that border be a bit thinner and straight.

The title of the page should be home, it should be Rides Visualizer