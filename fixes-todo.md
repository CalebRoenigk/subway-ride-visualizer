On the Rolling Stock Page the Ridden by Yard graph should probabbly do this on mobile view:
Show the name of the yard and car count on one line and the graph bar below that, so that each graph bar is easier to see and tell its proportions.

On the lookup page in the trainset widget all car cards should be the same size, you can see this issue on mobile. Also those train cars in that graphic that have logged rides and arent the current active lookup should allow you to click on them and perform a lookup on that number

With the current Rides by Line graph, the far right date should be the current days date, right now it only shows the last date there was a logged ride.